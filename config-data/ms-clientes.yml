server:
  port: 9395
spring:
 application:
  name: ms-clientes
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:9394eureka
  instance:
    hostname: localhost